# Aspire workspace

This folder (`C:\FrontEnd\sdh`) contains the local workspace for the Aspire project.

Files created automatically:
- `README.md` (this file)
- `package.json` (minimal placeholder)
- `.gitignore` (common ignores)

Next steps: initialize a Node project or scaffold a frontend (React/Vite) if you want.
